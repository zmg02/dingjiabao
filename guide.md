# 盯价宝 使用说明

> 版本 0.1 · 更新于 2026-09-01 · 有问题:[提 Issue](https://github.com/zmg02/dingjiabao/issues) 或爱发电私信

## 一、安装

| 方式 | 步骤 |
|---|---|
| Edge 商店(推荐,国内可直接访问) | [点此安装](https://microsoftedge.microsoft.com/addons/detail/ihnmgcceeglbajigfmfhaojcdmaolcjn) |
| Chrome 商店 | 审核中,上架后更新 |
| 手动安装(zip) | 下载 zip → 解压到一个**不会删除的文件夹** → 浏览器打开 `edge://extensions`(Chrome 为 `chrome://extensions`)→ 右上角打开「开发人员模式」→ 「加载解压缩的扩展」→ 选择解压后的文件夹 |

安装后建议把盯价宝图标**固定到工具栏**:点浏览器右上角的「扩展」拼图按钮 → 盯价宝旁边的「显示在工具栏」。

## 二、添加监控(3 步)

1. 打开一个**商品详情页**(Amazon、速卖通、1688、京东、淘宝、Shopify 独立站……任何网页都行)
2. 点工具栏的盯价宝图标 → 「在当前页选取要监控的元素」
3. 鼠标移到页面上,要监控的内容会高亮 → **点一下价格 / 库存 / 评分**(按 Esc 取消)

看到「已添加监控」通知就成功了。点价格的任何一部分都行(比如只点到 `138` 或 `.08`),盯价宝会自动找到完整的价格。

**不要在列表页 / 搜索页 / 首页选取。** 这些页面的同一个位置每次刷新可能是不同商品,监控会误报。

## 三、查看与管理

点图标 → 「打开监控列表」,侧边栏里每一条可以:

- 看当前值、上一次的值、上次检查时间、上次变化时间
- 改**检查间隔**:30 分钟 / 1 小时 / 6 小时 / 24 小时(免费版最短 6 小时)
- 「立即检查」:马上查一次,并从现在重新计时
- 「暂停 / 继续」、「✕ 删除」

浏览器右上角图标的**红色角标**是"有几条发生了变化",打开列表后清零。

## 四、检查机制(重要)

- 盯价宝在后台每 5 分钟醒来一次,对"距上次检查已经超过设定间隔"的项目重新检查,实际检查时刻最多比设定间隔晚几分钟。
- 检查方式是**只读**:先在后台静默请求页面;如果网站需要登录或价格由脚本延迟加载(如京东),会自动在后台打开一个标签页读取后关闭,你可能看到标签页闪一下,这是正常的。
- **浏览器完全关闭时不会检查。** 需要的话在 ⚙ 设置里打开「关闭浏览器后继续监控」,并确认浏览器设置里「关闭浏览器后继续运行后台扩展」是开着的(Edge:`edge://settings/system`;Chrome:`chrome://settings/system`)。开启后浏览器会在托盘保留一个后台进程。
- 淘宝 / 天猫需要浏览器已登录,并建议间隔 ≥ 6 小时,过于频繁可能触发平台验证。

## 五、免费版与 Pro

| | 免费版 | Pro(买断) |
|---|---|---|
| 监控项数量 | 3 个 | 不限 |
| 最短检查间隔 | 6 小时 | 30 分钟 |
| 导出历史 CSV | — | ✅ |
| 设备数 | 不限 | 不限 |
| 价格 | 永久免费 | ¥49(首发 ¥29,限前 50 名) |

购买:见 **[购买指引](buy)**(爱发电 ¥29,期限选 1 个月即可,填邮箱收激活码)→ 侧边栏 ⚙ → 粘贴激活码 → 「激活」。激活码离线校验,不需要联网、不绑定设备。

## 六、隐私

所有监控项、历史记录、激活码都只保存在你浏览器本地,不上传到任何服务器;扩展只访问你自己添加的页面。卸载扩展即删除全部数据。详见[隐私政策](privacy)。

## 七、常见问题

**添加时提示"请先切换到一个普通网页"**:当前标签页是浏览器内部页面或扩展页面,切到商品页再点。

**值显示 $0.00 或明显不是页面上的价格**:该网站的价格由脚本延迟加载,盯价宝通常会自动改用后台标签页读取;若仍不对,把该监控项的「读取:自动」改成「读取:标签页」。

**显示"读不到该元素"**:页面结构变了、需要登录、或该网站有反爬限制。先打开该商品页确认能正常看到价格,然后删掉重新选取。

**值里带了多余文字**:选取时点到了包含价格的大区块。删掉后重新选,尽量点最小的那个数字。

**同一个商品要监控价格和库存**:分别添加两次,一次点价格、一次点库存。

**换电脑 / 重装浏览器**:数据在本地,不会同步;Pro 激活码可以在新设备重新输入。
